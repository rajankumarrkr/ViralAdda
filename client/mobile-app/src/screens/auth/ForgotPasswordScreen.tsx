import React from 'react';
import { Text, View } from 'react-native';

export function ForgotPasswordScreen() {
  return <View style={{ padding: 16 }}><Text>Password reset will connect to the backend email provider in a later phase.</Text></View>;
}
